; ============================================================
;  FollowerTactics - restore the SELECTED follower
;  Select them, then:  bat ftclean
;
;  NON-DESTRUCTIVE, deliberately. These are real quest NPCs:
;  removeallitems would strip their gear and markfordelete would
;  remove them from the save permanently.
;
;  restoreav is the clean inverse of fthurt's damageav: it puts
;  the current value back without touching base.
;
;  Spawned creatures are not cleaned up here; their RefIDs are
;  not knowable in advance. Kill them, or select each and run
;  disable then markfordelete by hand.
; ============================================================

player.tgm

restoreav health 99999
restoreav magicka 99999
restoreav stamina 99999
resurrect 1
