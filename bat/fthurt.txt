; ============================================================
;  FollowerTactics - wound the SELECTED follower
;  Select them, then:  bat fthurt      then  bat ftstatus
;
;  Re-runnable; each run takes off another 100.
;
;  damageav applies to the DAMAGE modifier, which is what real
;  combat damage does. Never setav, modav or forceav on a
;  resource you intend to keep reading: those write the value
;  itself, corrupt max health, and can stop it regenerating.
;
;  No before/after print here -- readings in this same file could
;  run before the damage. Use ftstatus, as its own command.
; ============================================================

damageav health 100
damageav magicka 100
