; ============================================================
;  FollowerTactics - set up the PLAYER for a test session
;  Run once per session:  bat ftsetup
;
;  God mode, so the bears cannot reach you while you watch the
;  follower. Gold, so a hireling (Marcurio charges 500) can be
;  recruited through dialogue -- which is the only recruit that
;  fills the follower alias the cast route runs on. Everything
;  that configures a FOLLOWER is in ftmake, on a selection.
;
;  tgm TOGGLES. ftclean toggles it back at the end of a session.
; ============================================================

player.tgm
player.additem f 10000

; The player's own tactics (dev/PLAYER.md): a scroll to read from a
; hand, a shield and a sword for the bashes and the lent hand, and the
; three minor restore potions ftmake gives a follower.
player.additem 000A44AE 3
player.additem 00012EB6 1
player.additem 00012EB7 1
player.additem 0003EADE 3
player.additem 0003EAE1 3
player.additem 00039BE8 3
