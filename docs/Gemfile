# Used for local previews and the versioned GitHub Actions build:
#
#   bundle install
#   bundle exec jekyll serve
#
# The github-pages gem keeps the same dependency set as the original
# branch-based Pages build.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"
gem "rake"
