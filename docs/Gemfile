# Only for previewing the site locally:
#
#   bundle install
#   bundle exec jekyll serve
#
# GitHub Pages does not read this file — it builds the branch with its own
# pinned Jekyll. The gem below is that same set, so a local preview matches
# what gets published.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"
gem "rake"
