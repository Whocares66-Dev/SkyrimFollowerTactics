# Build output
build/
out/
dist/
install/
cmake-build*/
vcpkg_installed/
*.dll
*.pdb
*.lib
*.obj

# LLVM coverage output
*.profraw
*.profdata

# Jekyll / Sass generated output and local Bundler configuration
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
.bundle/
/docs/vendor/

# Nested local preview worktree
/site-preview/

# Machine-specific assistant settings; shared skills remain trackable
/.claude/settings.local.json
/.claude/settings.json

# IDE
.vs/
.idea/
.vscode/settings.json
CMakeUserPresets.json

# Skyrim
*.bsa
*.ess
*.skse

# Downloaded tooling archives
*.7z
*.zip

# Local backups of vanilla game files
backup/

# Mod Organizer 2 instance data lives inside this repo. Its mods/ and
# downloads/ folders can reach many GB and must never be committed.
MO2/

# Third-party tooling unpacked here
SKSE/
AddressLibrary/
CKPE-*/
CreationKitDowngradePatcher/

# Downloaded mod archives (SKSE Menu Framework and its dependencies).
# Kept out of the repo so a clone stays small; see extern/ for what is needed.
# Anchored with a leading slash: unanchored, this pattern matches at ANY depth
# and would swallow extern/SKSEMenuFramework/ along with its README.
/SKSEMenuFramework/

# The SKSE Menu Framework SDK header is a build dependency but NOT ours to
# redistribute: it carries no licence notice, and it is a one-file download from
# the mod's Nexus page. extern/SKSEMenuFramework/README.md says how to get it.
extern/SKSEMenuFramework/SKSEMenuFramework.h
SSEEdit/
CrashLogger/


# A local scratchpad for review notes, not part of the tree.
dev/REVIEW.md
